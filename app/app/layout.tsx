
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Header } from '@/components/header'
import { Footer } from '@/components/footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Bonanza Camping - Premium Outdoor Adventures',
  description: 'Experience the ultimate camping adventure at Bonanza Camping. Premium campsites, outdoor activities, and unforgettable memories in nature.',
  keywords: 'camping, outdoor adventures, nature, hiking, fishing, campfire, wilderness',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
